{
    "version": "0.2.0",
    "justMyCode": false,
    "configurations": [
        {
            "justMyCode": false,
            "name": "Python: Aktuelle Datei",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            // make sure that local virtual env is used
            "pythonPath": "${workspaceFolder}/.venv/bin/python"
        }
    ]
}